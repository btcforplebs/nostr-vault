*.bak
*.bak2
/HavenApp/build
/HavenApp/build_data
# Root-level scratch notes only. A blanket *.txt swallowed
# NostrVault/app/src/main/CMakeLists.txt, which app/build.gradle.kts
# requires, so a fresh clone could not assemble the Android APK.
/*.txt
/build
/build_data
*.build

# Xcode user state
*.xcuserstate

# macOS
.DS_Store

# Planning docs
RENAME_PLAN.md
DM_PLAN.md

# FIPS_FFI_PLAN.md and FIPS_INTEGRATION_ROADMAP.md are deliberately tracked:
# they are the architecture documents for fips-bridge/ and need to version
# alongside the code they describe.

# Build artifacts
/HavenApp/SilentPaymentsKit/build/

# Go vendor (managed by go modules)
/haven-go/vendor/

# Android (NostrVault)
/NostrVault/local.properties
/NostrVault/.gradle/
/NostrVault/.kotlin/
/NostrVault/build/
/NostrVault/app/build/
/NostrVault/app/.cxx/
/NostrVault/app/src/main/jniLibs/

# Environment secrets
push-server/.env
.env
NostrVault/.env
HavenApp/Monolithic split.md
HavenApp/Roadmap V1.md
HavenApp/Roadmap V2.md
HavenApp/HavenApp/Services/BUD-14.code-workspace

# Claude Code local state (worktrees, memory, jobs)
.claude/

# Release tooling
dist/
.release.env
/HavenApp/MediaLogicTests/.build/

# Compiled Go relay artifacts — rebuilt from haven-go/ by
# HavenApp/HavenApp/App/build_haven.sh into HavenApp/build/, never committed.
# (HavenApp/Resources/haven stays tracked: project.yml bundles it as a resource.)
/libhaven.a
/haven-go/libhaven.a
/haven-go/haven.dylib
/HavenApp/HavenApp/Services/haven.dylib
*.so

# Xcode per-user state
xcuserdata/
